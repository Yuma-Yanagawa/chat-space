.chat-group-form
  %h1 新規チャットグループ
  = form_for @group do |f|
    .chat-group-form__errors
      %h2
        = "#{@group.errors.count}件のエラーが発生しました。"
        %ul
          %li エラーです
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
        = f.label :name, "グループ名", class: "chat-group-form__label"
      .chat-group-form__field--right
        = f.text_field :name, class: "chat-group-form__input", placeholder: "グループ名を入力してください"
        = f.collection_check_boxes(:user_ids, User.all, :id, :name) do |b|
          =b.label {b.check_box + b.text}
        / この部分はインクリメンタルサーチ（ユーザー追加の非同期化のときに使用します
        /
        -# = f.label :chat_group, "チャットメンバーを追加"
        -# = f.text_field :chat_group, class: ".chat-group-form__input", placeholder: "追加したいユーザー名を入力してください"
      .chat-group-form__field.clearfix
        .chat-group-form__field--left
        .chat-group-form__field--right
          = f.label :chat_group, class: "chat-group-user__label"
          -# = f.text_field :chat_group[user_ids][]
          = f.submit "登録する", class: "chat-group-user__action-btn"
